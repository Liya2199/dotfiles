-- 在你的 LazyVim 插件配置文件夹中（例如 lua/plugins/copilot.lua）
return {
  "zbirenbaum/copilot.lua",
  opts = {
    panel = {
      enabled = false, -- 默认禁用占用了太多内存使用:Copilot enable开启
      layout = {
        position = "right", -- | "top" | "left" | "bottom"
        ratio = 0.4,
      },
    },
    suggestion = {
      enabled = true,
      auto_trigger = true,
    },
    filetypes = {
      ["*"] = false, -- 允许所有文件类型使用 Copilot
      -- 你也可以禁用特定的文件类型，例如：
      -- ["markdown"] = false,
      python = true,
      rust = true,
      lua = true,
      json = true,
      markdown = false,
      txt = false,
    },
  },
}
